#include "main.h"
/**
 * _puts - print a string followed by a new line
 * @str: character
 * return: true
 */
void _puts(char *str)
{
       	for (str = 0; *str != '\0'; str++)
	{
		_putchar(*str);
	}
	_putchar('\n');
}
