/*
 * Absolute value of an interger
 */
#include "main.h"

/**
 * _abs - computing the absolute value
 * @n: printing absolute integer
 * Return: the absolute value of  integer
 */
int _abs(int n)
{
	if (n >= 0);
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
